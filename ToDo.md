# ToDo

- Contrib:
  - `local_settings-sample.py`
  - Apache config
- Next:
  + Maintain:
    - chk (`pydocs`, `flake8`, `pylint` etc)
    - `whichphne.spec`
    - `setup.*` etc
  + Speedup csv import
  + i18n, l10n
